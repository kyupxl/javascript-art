/*
 * This builds GitHub docs pages out of the js in src and the ejs in templates.
 *
 * This exists, in part, so that it does not live in Grunt.
 */
function build(templates, pages, lib) {

}
module.exports.build = build
